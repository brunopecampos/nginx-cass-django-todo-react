FROM python
COPY ./backend ./app
WORKDIR ./app
# install pipenv
RUN apt update && apt install -y pipenv
# set enviroment
RUN pipenv shell 
# install dependencies
RUN pipenv install
CMD ["python", "manage.py", "runserver"]